import { createTheme } from '@mui/material';

export const streamTheme= createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#cf5c36',
    },
    secondary: {
      main: '#f50057',
    },
    background: {
      default: '#1BA2B1',
    },
  },
});